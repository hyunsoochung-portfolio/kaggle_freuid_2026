name: a100_effnetv2m
seed: 42

# data
data_dir: data
image_size: 384
val_fraction: 0.1

# model
backbone: tf_efficientnetv2_m.in21k
pretrained: true

# train — tuned for A100 (40 GB VRAM)
epochs: 20
batch_size: 64
lr: 0.0003
weight_decay: 0.0001
num_workers: 4
