# Technical report

Mandatory deliverable for prize eligibility: method, data handling, training, and
reproducibility instructions sufficient for organizers to re-run our ranked result.

Draft the report here (Markdown or LaTeX). See `experiments.md` for the run log that
feeds the results section.

## Outline (fill in)

1. Problem & approach
2. Data: splits, preprocessing, augmentation (physical / GenAI / print-capture robustness)
3. Model & training
4. Results: local AuDET / APCER@1%BPCER + leaderboard
5. Reproducibility: exact commands, env (`uv.lock`), seeds, configs
