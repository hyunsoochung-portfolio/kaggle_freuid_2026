# Copy to .env and fill in real values. .env is gitignored -- never commit real secrets.

# Hugging Face token with access to the gated DINOv3 repo. Required to load
# backbone_name: dinov3_vitb16 (see src/freuid/backbone.py). Get one at
# https://huggingface.co/settings/tokens, and accept the license at
# https://huggingface.co/facebook/dinov3-vitb16-pretrain-lvd1689m first.
HF_TOKEN=
